// Default server configuration values for the backend.
import type { ServerConfig } from '../types/index.js';

export const defaultServerConfig: ServerConfig = {
  backendHost: '127.0.0.1',
  backendPort: 3301,
};
